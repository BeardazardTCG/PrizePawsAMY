import { competitions, winners } from "@/lib/data";
import { ButtonLink, Card, PawAvatar, Section } from "@/components/ui";

export default function Home() {
  const featured = competitions[0];
  return <><section className="px-5 pb-8 pt-7 sm:px-8 md:grid md:grid-cols-[1.1fr_0.9fr] md:items-center md:gap-8 md:py-14"><div><p className="inline-flex rounded-full bg-white px-4 py-2 text-sm font-black text-terracotta shadow-sm">Monthly dog photo competitions</p><h1 className="mt-5 text-5xl font-black leading-[0.95] tracking-tight text-cocoa md:text-7xl">Win rosettes for pawsome photos.</h1><p className="mt-5 max-w-xl text-lg leading-8 text-cocoa/70">A mobile-first competition app for dog people: browse classes, enter your pup, collect Paw Points, and celebrate winners in a polished community gallery.</p><div className="mt-7 flex flex-wrap gap-3"><ButtonLink href="/competitions">Browse classes</ButtonLink><ButtonLink href="/upload" variant="secondary">Start entry</ButtonLink></div></div><Card className="mt-8 overflow-hidden bg-gradient-to-br from-white to-biscuit/50 md:mt-0"><div className={`rounded-[1.75rem] bg-gradient-to-br ${featured.color} p-5`}><PawAvatar label="featured dog" /><p className="mt-5 text-sm font-black uppercase tracking-widest text-cocoa/55">Featured competition</p><h2 className="mt-2 text-3xl font-black">{featured.title}</h2><p className="mt-2 text-cocoa/70">{featured.description}</p><div className="mt-5 grid grid-cols-2 gap-3 text-sm font-bold"><span className="rounded-2xl bg-white/70 p-3">🏆 {featured.prize}</span><span className="rounded-2xl bg-white/70 p-3">⏰ Closes {featured.closes}</span></div></div></Card></section><Section eyebrow="How it works" title="Enter in under a minute"><div className="grid gap-4 md:grid-cols-3">{["Pick a monthly class", "Upload your best dog photo", "Track results, points, and rosettes"].map((item, i) => <Card key={item}><span className="text-3xl font-black text-terracotta">0{i + 1}</span><p className="mt-3 text-xl font-black">{item}</p></Card>)}</div></Section><Section eyebrow="Latest rosettes" title="Winner spotlight"><div className="grid gap-4 md:grid-cols-3">{winners.map(w => <Card key={w.dog}><span className={`inline-flex rounded-full ${w.ribbon} px-3 py-1 text-xs font-black`}>{w.place} place</span><h3 className="mt-3 text-2xl font-black">{w.dog}</h3><p className="font-bold text-terracotta">{w.className}</p><p className="mt-2 text-cocoa/65">{w.caption}</p></Card>)}</div></Section></>;
}
